#ifndef _tTESTER_H_
#define _tTESTER_H_

#include "Solver.h"
#include "Parser.h"
/*
// Run tests on the solver
class Tester : private Solver
{


public:
	void addToUP(string svariable, string svalue);
	void printValue(string svariable);
	void propagate();

	void addLearnedSet(string type, vector<string> vars);

	Tester(Circuit *c);

	void runTests();
};

*/
#endif
